import React from 'react'
import { TouchableRipple as RNPaperTouchableRipple } from 'react-native-paper'

const TouchableRipple = (props) => {
  return (
    <RNPaperTouchableRipple {...props} />
  )
}

export default TouchableRipple